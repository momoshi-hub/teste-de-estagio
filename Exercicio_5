# Definindo a string
s = "Hello, world!"

# Invertendo os caracteres
s_invertida = ""
for i in range(len(s)-1, -1, -1):
    s_invertida += s[i]

# Imprimindo a string invertida
print(s_invertida)


##foi necessario criar um loop para poder fazer o s 'de string ' ficar invertido aplicando o menos 1 no laço
##gerando a resposta !DLROW OLLEH